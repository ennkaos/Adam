import { StartingTimeSlotPipe } from './starting-time-slot.pipe';

describe('StartingTimeSlotPipe', () => {
  it('create an instance', () => {
    const pipe = new StartingTimeSlotPipe();
    expect(pipe).toBeTruthy();
  });
});
